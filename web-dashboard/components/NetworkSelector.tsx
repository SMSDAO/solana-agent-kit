'use client'

export function NetworkSelector() {
  // Network selection UI removed because it did not affect the actual wallet network.
  // If network switching is implemented in the future, this component can be wired up
  // to the WalletProvider or relevant network adapter.
  return null
}
